{
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && serve dist"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "date-fns": "^4.1.0",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.2"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
